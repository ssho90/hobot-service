name: Deploy to AWS EC2

on:
  push:
    branches:
      - main  # main 브랜치에 push될 때 배포
  workflow_dispatch:  # 수동 실행도 가능

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup SSH
      env:
        EC2_HOST: ${{ vars.EC2_HOST }}
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        if [ -z "$EC2_HOST" ]; then
          echo "❌ Error: EC2_HOST variable is not set"
          exit 1
        fi
        echo "🔍 Attempting to connect to EC2 host: ${EC2_HOST}"
        if ! ssh-keyscan -H ${EC2_HOST} >> ~/.ssh/known_hosts 2>&1; then
          echo "❌ Error: Failed to connect to EC2 host ${EC2_HOST}"
          echo "Please check:"
          echo "  1. EC2 instance is running"
          echo "  2. IP address is correct (current: ${EC2_HOST})"
          echo "  3. Security Group allows SSH (port 22) from GitHub Actions IPs"
          echo "  4. Network connectivity"
          exit 1
        fi
        echo "✅ Successfully added ${EC2_HOST} to known_hosts"
    
    - name: Deploy to EC2
      env:
        EC2_HOST: ${{ vars.EC2_HOST }}
        EC2_USER: ${{ vars.EC2_USER }}
        DEPLOY_PATH: ${{ vars.EC2_DEPLOYMENT_PATH || '/home/ec2-user/hobot-service' }}
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} << ENDSSH
          set -e
          
          export DEPLOY_PATH="${DEPLOY_PATH}"
          export GH_TOKEN="${GH_TOKEN}"
          
          echo "🚀 Starting deployment..."
          echo "📁 Deploy path: \${DEPLOY_PATH}"
          
          # 배포 디렉토리로 이동
          cd \${DEPLOY_PATH} || {
            echo "❌ Deploy directory not found. Creating..."
            mkdir -p \${DEPLOY_PATH}
            cd \${DEPLOY_PATH}
          }
          
          # Git pull (또는 초기 클론)
          if [ -d ".git" ]; then
            echo "📥 Pulling latest changes..."
            git pull https://\${GH_TOKEN}@github.com/${{ github.repository }}.git main || true
          else
            echo "📥 Cloning repository..."
            # 기존 디렉토리가 비어있지 않으면 백업 후 클론
            if [ "\$(ls -A . 2>/dev/null)" ]; then
              echo "⚠️ Directory is not empty, backing up existing files..."
              cd ..
              BACKUP_NAME="\${DEPLOY_PATH}.backup.\$(date +%s)"
              echo "📦 Backing up to: \${BACKUP_NAME}"
              mv \${DEPLOY_PATH} \${BACKUP_NAME}
              mkdir -p \${DEPLOY_PATH}
              cd \${DEPLOY_PATH}
            fi
            git clone https://\${GH_TOKEN}@github.com/${{ github.repository }}.git .
          fi
          
          # Backend 배포
          echo "🔧 Setting up backend..."
          cd hobot
          
          # Python 가상환경 설정
          if [ ! -d "venv" ]; then
            echo "🐍 Creating virtual environment..."
            # ensurepip가 실패할 수 있으므로 --without-pip 옵션 사용 후 pip를 별도로 설치
            if ! python3 -m venv --without-pip venv 2>/dev/null; then
              echo "⚠️ --without-pip failed, trying standard venv..."
              if ! python3 -m venv venv 2>/dev/null; then
                echo "❌ Failed to create virtual environment"
                exit 1
              fi
            fi
            echo "✅ Virtual environment created"
          fi
          
          # 가상환경 활성화 확인
          if [ ! -f "venv/bin/activate" ]; then
            echo "❌ Error: venv/bin/activate not found"
            echo "Virtual environment structure:"
            ls -la venv/ 2>/dev/null || echo "venv directory does not exist"
            exit 1
          fi
          
          source venv/bin/activate
          
          # pip가 없으면 설치
          if ! command -v pip &> /dev/null; then
            echo "📦 Installing pip..."
            curl -s https://bootstrap.pypa.io/get-pip.py -o get-pip.py || {
              echo "⚠️ Failed to download get-pip.py, trying ensurepip..."
              python3 -m ensurepip --upgrade --default-pip || {
                echo "❌ Failed to install pip"
                exit 1
              }
            }
            if [ -f "get-pip.py" ]; then
              python3 get-pip.py --user || python3 get-pip.py || {
                echo "⚠️ get-pip.py failed, trying ensurepip..."
                python3 -m ensurepip --upgrade --default-pip
              }
              rm -f get-pip.py
              # pip를 가상환경에 복사
              if [ -f ~/.local/bin/pip ]; then
                cp ~/.local/bin/pip venv/bin/pip 2>/dev/null || true
                cp -r ~/.local/lib/python*/site-packages/pip* venv/lib/python*/site-packages/ 2>/dev/null || true
              fi
            fi
          fi
          
          # 의존성 설치
          echo "📦 Installing Python dependencies..."
          pip install --upgrade pip || python3 -m ensurepip --upgrade || echo "⚠️ pip upgrade failed, continuing..."
          pip install -r requirements.txt
          
          # 전략 초기화 (KIS와 Upbit를 PAUSE로 설정)
          echo "⏸️ Initializing strategies to PAUSE..."
          python3 service/utils/init_strategy_pause.py
          
          # 로그 디렉토리 생성
          mkdir -p logs
          
          # Systemd 서비스 설정 (선택사항)
          if [ -f "../.github/deploy/hobot.service" ]; then
            echo "⚙️ Setting up systemd service..."
            sudo cp ../.github/deploy/hobot.service /etc/systemd/system/hobot.service
            sudo sed -i "s|/home/ubuntu/hobot-service|\${DEPLOY_PATH}|g" /etc/systemd/system/hobot.service
            sudo systemctl daemon-reload
            sudo systemctl enable hobot.service
            echo "🔄 Restarting service..."
            sudo systemctl restart hobot.service
            echo "✅ Service restarted!"
          else
            # Systemd 없이 직접 실행
            echo "🛑 Stopping existing services..."
            PORT=8991
            EXISTING_PIDS=\$(lsof -ti :\$PORT 2>/dev/null || true)
            if [ ! -z "\$EXISTING_PIDS" ]; then
              echo "Killing processes on port \$PORT: \$EXISTING_PIDS"
              kill -9 \$EXISTING_PIDS 2>/dev/null || true
              sleep 2
            fi
            
            # Gunicorn으로 서버 시작 (백그라운드)
            echo "🚀 Starting FastAPI server with Gunicorn..."
            nohup python3 -m gunicorn -c gunicorn.conf.py asgi:asgi_app > /dev/null 2>&1 &
          fi
          
          echo "✅ Backend deployment completed!"
          
          # Frontend 배포 (선택사항)
          if [ -d "../hobot-ui" ]; then
            echo "🎨 Setting up frontend..."
            cd ../hobot-ui
            
            # Node.js 확인
            if command -v node &> /dev/null; then
              echo "📦 Installing frontend dependencies..."
              npm install --production
              
              # 빌드 (프로덕션)
              echo "🏗️ Building frontend..."
              npm run build || echo "⚠️ Build script not found, skipping..."
            else
              echo "⚠️ Node.js not found, skipping frontend deployment"
            fi
          fi
          
          echo "🎉 Deployment completed successfully!"
        ENDSSH
    
    - name: Health Check
      env:
        EC2_HOST: ${{ vars.EC2_HOST }}
        EC2_USER: ${{ vars.EC2_USER }}
      run: |
        sleep 10  # 서버 시작 대기
        ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} << 'ENDSSH'
          echo "🔍 Checking server health..."
          if curl -f http://localhost:8991/ > /dev/null 2>&1; then
            echo "✅ Backend server is healthy!"
          else
            echo "❌ Backend server health check failed"
            exit 1
          fi
        ENDSSH

